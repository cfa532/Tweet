package com.fireshare.tweet.tweet

import androidx.compose.runtime.Composable
import com.fireshare.tweet.datamodel.MimeiId

@Composable
fun CommentItem(tweetId: MimeiId) {
    // each comment is tweet itself.
}